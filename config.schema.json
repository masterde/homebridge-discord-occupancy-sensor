{
  "pluginAlias": "DiscordOccupancySensor",
  "pluginType": "accessory",
  "headerDisplay": "Homebridge plugin that creates an occupancy sensor in HomeKit based on the online status of a Discord user",
  "footerDisplay": "Created by @xyz",
  "schema": {
    "type": "object",
    "properties": {
      "debug": {
        "title": "Enable Debug Logs",
        "description": "When checked, the plugin will produce extra logs for debugging purposes",
        "type": "boolean",
        "default": false,
        "required": false
      },
      "token": {
        "title": "Discord Bot Token",
        "description": "The token for your Discord bot",
        "type": "string",
        "required": true
      },
      "userId": {
        "title": "Discord User ID",
        "description": "The ID of the Discord user to monitor",
        "type": "string",
        "required": true
      },
      "name": {
        "title": "Sensor Name",
        "description": "The name of the occupancy sensor to create in HomeKit",
        "type": "string",
        "required": true
      }
    }
  },
  "layout": [
    {
      "key": "token"
    },
    {
      "key": "userId"
    },
    {
      "key": "name"
    },
    {
      "key": "debug"
    }
  ]
}