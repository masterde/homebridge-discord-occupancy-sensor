{
  "pluginAlias": "DiscordPresence",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Homebridge plugin that provides occupancy sensors for Discord user presence in a specified guild",
  "footerDisplay": "Created by @xyz",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "description": "The name of the occupancy sensor accessory",
        "type": "string",
        "default": "Discord Occupancy Sensor"
      },
      "token": {
        "title": "Discord Bot Token",
        "description": "The Discord bot token used to authenticate with the Discord API",
        "type": "string",
        "required": true
      },
      "guildId": {
        "title": "Discord Guild ID",
        "description": "The ID of the Discord guild to monitor for presence",
        "type": "string",
        "required": true
      },
      "debug": {
        "title": "Enable Debug Logging",
        "description": "When checked, the plugin will produce extra logs for debugging purposes",
        "type": "boolean",
        "default": false,
        "required": false
      }
    },
    "required": [
      "token",
      "guildId"
    ]
  },
  "layout": [
    {
      "key": "name"
    },
    {
      "key": "token"
    },
    {
      "key": "guildId"
    },
    {
      "key": "debug"
    }
  ]
}